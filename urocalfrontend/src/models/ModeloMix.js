export default class Mix {
    constructor(mixid, almacenamientoid, cosechaid) {
        this.mixid = mixid;
        this.almacenamientoid = almacenamientoid;
        this.cosechaid = cosechaid;
    }
}